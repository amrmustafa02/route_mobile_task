import 'package:flutter/material.dart';

class PostsDetailsBody extends StatelessWidget {

  const PostsDetailsBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [

      ],
    );
  }
}
